# Mavis 用户偏好规则（2026-08-06）

> **同步状态**：本文件已同步到 Mavis `user memory`（跨项目/跨会话生效，机器可读）。两份内容应保持一致——修改规则请同时更新两边。
> **最后更新**：2026-08-06 14:28
> **规则数**：4 条
> **存放位置**：`E:/AI相关/预研究/202608/03_输出/Mavis_用户偏好_2026-08-06.md`（**不会**被部署到云端 hub）

---

## 目录

1. [删除文件需用户确认](#1-删除文件需用户确认preference)
2. [多智能体协作 + 命名空间隔离](#2-多智能体协作--命名空间隔离preference)
3. [WB 工作流文件归属](#3-wb-工作流文件归属workflow)
4. [来源 URL 可达性过滤](#4-来源-url-可达性过滤preference)
5. [附录：待补充信息](#5-附录待补充信息)

---

## 1. 删除文件需用户确认（preference）

**规则**：所有需要删除的文件（包括但不限于 `Remove-Item` / `mavis-trash` / `bash rm` / `mv` / `unlink` / 文件改名为 `.migrated` 后缀 / 卸载应用 / 清理缓存 / 删除目录），**必须先与用户确认**，没有得到用户明确同意不能删除任何文件。

**证据/原因**：2026-08-06 用户明确要求"所有的需要删除的文件跟我确认下，没有我的同意不能删除任意文件"，确保删除操作安全，避免误删用户工作产物。

**适用**：所有任务、所有项目、所有工具调用。当 cron / 自动化流程可能触发删除动作时，先列出待删清单让用户拍板；用户确认后再执行；用户拒绝则跳过并备注。

**操作流程**：
1. 发现/被要求删除某个文件时
2. 列出**待删清单**（路径 + 大小 + 类型 + 删除原因 + 风险评估）
3. 等待用户明确同意（"删" / "可以" / "OK" / "同意"）
4. 用户同意后再执行
5. 用户拒绝 → 跳过 + 备注原因

---

## 2. 多智能体协作 + 命名空间隔离（preference）

**规则**：
1. 用户任务可能由**多个智能体协作**完成（2026-08-06 14:10 明确）
2. 我的产出按我的命名约定（`MM-*` 前缀 + 文件名 = `MM-YYYY-MM-DD_daily.md` / `MM-YYYY-MM-DD_硬件日报.html`），**不重名/不冲突**其他智能体
3. **不能删除或覆盖其他智能体生产的文件**（输出/日志/配置/缓存/临时/任何命名空间下文件）
4. 疑似其他智能体产出 → **先报告用户确认**，不擅自删/改/覆盖
5. 不确定文件归属时，**先列元信息让用户判断**（路径 + 大小 + 修改时间 + 创建进程/来源）

**证据/原因**：今日（2026-08-06）10:23-10:34 之间，`03_输出/` 下我写的 4 个 `MM-*` 文件（8-05 / 8-06 的 markdown + HTML 看板）被某个清理脚本/其他智能体**精确按 `MM-` 前缀清扫**——用户的 WB 工作底稿、`index.html` 都没动。印证了"多智能体协作"风险。

**适用**：所有多智能体协作场景；单智能体独立任务时同样适用（避免误删任何文件）。与规则 #1 互为补充。

**当前已知命名空间**：
- **Mavis（本智能体）**：`MM-*` 前缀
- **用户 WB（WorkBench）工作流**：`WB_*` 前缀 或 `硬件日报入口.html` 等语义名（详见规则 #3）
- **其他智能体**：未知，待用户补充（见附录）

---

## 3. WB 工作流文件归属（workflow）

**规则**：识别为**用户自己产出**的 WB（WorkBench）工作流文件，**不删/不改/不覆盖**。

**WB 命名约定**：
1. **带 WB 前缀**：`WB_YYYY-MM-DD_*.html` 等
2. **无 WB 前缀但语义明确**：`硬件日报入口.html` 等
3. **共性**：都位于 `03_输出/` 目录，跟用户"工作底稿"配套

**当前识别清单**（截至 2026-08-06）：
- `E:/AI相关/预研究/202608/03_输出/WB_2026-08-05_硬件看板.html`
- `E:/AI相关/预研究/202608/03_输出/WB_2026-08-06_硬件看板.html`
- `E:/AI相关/预研究/202608/03_输出/硬件日报入口.html`
- `E:/AI相关/预研究/202608/03_输出/index.html`（用户 10:11 写的看板索引）

**适用**：所有 Mavis 任务。新增 WB 文件时，**用户通知我后**加入白名单。

---

## 4. 来源 URL 可达性过滤（preference）

**规则**：网页版（HTML 看板 / 入口页 / 任何带 URL 的网页交付物）的"信源 URL"必须**事前可达性检查**——
1. 写入前测所有 `sources` URL（HTTP 200/2xx/3xx 保留；4xx/5xx/timeout 过滤）
2. 不可达 URL 在 HTML 标"（链接已失效）"或直接删除
3. 关键源（>=2 印证源中的 1 个）不可达 → Watchlist 标注"源链接失效"
4. 信源描述里只保留可达 URL + 来源域名（如"腾讯网"、"快科技"）

**证据/原因**：2026-08-06 14:24 用户点开 8-06 HTML 看板某些信源 URL 发现打不开（IT 之家 8-04 首发稿返回 404），明确要求"打不开的网页一律过滤掉"。

**适用**：所有 Mavis 生成的 HTML 网页交付物（日报 / 看板 / 引文页 / 报告等）。

**工具与方法**：
- **单条测**：`web_fetch` 工具（GET 请求 + 拿状态码）
- **大批量测**：PowerShell 并行
  ```powershell
  $urls | ForEach-Object -Parallel {
      try {
          $resp = Invoke-WebRequest -Uri $_ -Method Head -TimeoutSec 10 -UseBasicParsing
          [int]$resp.StatusCode
      } catch { -1 }
  } -ThrottleLimit 12
  ```
  （或用 `Start-Job` 在 PowerShell 5.1 兼容）

**实测参考**（2026-08-06 14:24 测 8-06 HTML 的 36 条信源）：
- 35 条 200 OK
- 1 条 404（`https://www.ithome.com/0/893/001.htm` IT 之家 8-04 首发稿）
- 已过滤 + 改 `sourceCount 4→3` + `pendingSources` 加失效标注

---

## 5. 附录：待补充信息

### 其他智能体的命名约定（规则 #2 依赖项）

**当前状态**：**未知**

**待用户提供**：
- 是否有 Gemini / GPT / Claude / Kimi / 智谱 / 通义 / 文心 / 星火 等其他智能体在用？
- 每个用什么文件前缀？什么目录？什么后缀？
- 用户多智能体协作的典型工作流是什么？

**已知**：
- Mavis（本智能体）：`MM-*` 前缀
- 用户 WB：`WB_*` 前缀 或 `硬件日报入口.html` 等

### cron prompt 升级建议（待用户拍板）

明早 cron（2026-08-07 10:00）跑日报时，建议在**第 6 步（写 HTML 看板）之前**加一步：批量测所有 sources URL，过滤 4xx/5xx/timeout 后再 write。这样 8-07 起的每日看板**自动跳过失效链接**，不用事后手动修。

**当前状态**：**未升级**（用户 14:25 说"其他先放着再说"）

---

## 更新记录

| 时间 | 更新 | 原因 |
|---|---|---|
| 2026-08-06 10:23 | 规则 #1 触发 | 用户要求"删除文件需确认" |
| 2026-08-06 14:10 | 规则 #2 触发 | 用户要求"多智能体协作 + 命名空间隔离" |
| 2026-08-06 14:18 | 规则 #3 触发 | 用户确认 `硬件日报入口.html` 是 WB 产出 |
| 2026-08-06 14:24 | 规则 #4 触发 | 用户要求"打不开的网页一律过滤" |
| 2026-08-06 14:28 | 本 MD 文件首次生成 | 用户要求"记录到 MD + 全局记忆文件" |

---

**最后更新**：2026-08-06 14:28 by Mavis
